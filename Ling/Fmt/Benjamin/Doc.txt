The Language Benjamin
BNF Converter


%This txt2tags file is machine-generated by the BNF-converter
%Process by txt2tags to generate html or latex



This document was automatically generated by the //BNF-Converter//. It was generated together with the lexer, the parser, and the abstract syntax module, which guarantees that the document matches with the implementation of the language (provided no hand-hacking has taken place).

==The lexical structure of Benjamin==

===Literals===
Integer literals //Integer// are nonempty sequences of digits.


Double-precision float literals //Double// have the structure
indicated by the regular expression ``digit+ '.' digit+ ('e' ('-')? digit+)?`` i.e.\
two sequences of digits separated by a decimal point, optionally
followed by an unsigned or negative exponent.


String literals //String// have the form
``"``//x//``"``}, where //x// is any sequence of any characters
except ``"`` unless preceded by ``\``.


Character literals //Char// have the form
``'``//c//``'``, where //c// is any single character.





Name literals are recognized by the regular expression
`````["_"] (letter | ["-+*/%:=&|"] | digit)* (letter | ["-+*/%:=&|"]) (letter | ["-+*/%:=&|'"] | digit)* ["_"] | ["_"] | letter (letter | ["_-'"] | digit)*`````

OpName literals are recognized by the regular expression
`````(letter | ["-+*/%"] | digit)* ["-+*/%"] (letter | ["-+*/%_'"] | digit)* | (letter | ["-+*/%:=&|"] | digit)* ["-+*/%:=&|"] ["-+*/%:=&|"] (letter | ["-+*/%_:=&|'"] | digit)*`````


===Reserved words and symbols===
The set of reserved words is the set of terminals appearing in the grammar. Those reserved words that consist of non-letter characters are called symbols, and they are treated in a different way from those that are similar to identifiers. The lexer follows rules familiar from languages like Haskell, C, and Java, including longest match and spacing conventions.

The reserved words used in Benjamin are the following:
  | ``Type`` | ``as`` | ``assert`` | ``case``
  | ``data`` | ``end`` | ``fwd`` | ``in``
  | ``let`` | ``new`` | ``of`` | ``proc``
  | ``recv`` | ``send`` | ``slice`` | ``split``

The symbols used in Benjamin are the following:
  | = | : | ` | |
  | , | ( | ) | ->
  | < | > | { | }
  | [ | ] | [: | :]
  | ! | ? | ~ | <-
  | -o | ** | \ | .
  | ; | <= | @ | ^
  | new/ |  |  | 

===Comments===
Single-line comments begin with --.Multiple-line comments are  enclosed with {- and -}.

==The syntactic structure of Benjamin==
Non-terminals are enclosed between < and >. 
The symbols -> (production),  **|**  (union) 
and **eps** (empty rule) belong to the BNF notation. 
All other symbols are terminals.

  | //Program// | -> | //[Dec]// 
  | //Dec// | -> | //Name// //OptSig// ``=`` //Term// 
  |  |  **|**  | //Name// ``:`` //Term// 
  |  |  **|**  | ``data`` //Name// ``=`` //[ConName]// 
  |  |  **|**  | ``assert`` //Assertion// 
  | //Assertion// | -> | //Term// ``=`` //Term// //OptSig// 
  | //ConName// | -> | ````` //Name// 
  | //[ConName]// | -> | **eps**
  |  |  **|**  | //ConName// 
  |  |  **|**  | //ConName// ``|`` //[ConName]// 
  | //OptSig// | -> | **eps**
  |  |  **|**  | ``:`` //Term// 
  | //[Dec]// | -> | **eps**
  |  |  **|**  | //Dec// 
  |  |  **|**  | //Dec// ``,`` //[Dec]// 
  | //VarDec// | -> | ``(`` //Name// //OptSig// ``)`` 
  | //ChanDec// | -> | //Name// //OptRepl// //OptSession// 
  | //[ChanDec]// | -> | **eps**
  |  |  **|**  | //ChanDec// 
  |  |  **|**  | //ChanDec// ``,`` //[ChanDec]// 
  | //Branch// | -> | //ConName// ``->`` //Term// 
  | //[Branch]// | -> | **eps**
  |  |  **|**  | //Branch// 
  |  |  **|**  | //Branch// ``,`` //[Branch]// 
  | //Literal// | -> | //Integer// 
  |  |  **|**  | //Double// 
  |  |  **|**  | //String// 
  |  |  **|**  | //Char// 
  | //ATerm// | -> | //Name// 
  |  |  **|**  | //OpName// 
  |  |  **|**  | //Literal// 
  |  |  **|**  | //ConName// 
  |  |  **|**  | ``Type`` 
  |  |  **|**  | ``<`` //[RSession]// ``>`` 
  |  |  **|**  | ``(`` //Term// //OptSig// ``)`` 
  |  |  **|**  | ``end`` 
  |  |  **|**  | ``{`` //[RSession]// ``}`` 
  |  |  **|**  | ``[`` //[RSession]// ``]`` 
  |  |  **|**  | ``[:`` //[RSession]// ``:]`` 
  | //[ATerm]// | -> | **eps**
  |  |  **|**  | //ATerm// //[ATerm]// 
  | //Term3// | -> | //ATerm// //[ATerm]// 
  | //Term2// | -> | ``case`` //Term// ``of`` ``{`` //[Branch]// ``}`` 
  |  |  **|**  | ``!`` //Term3// //CSession// 
  |  |  **|**  | ``?`` //Term3// //CSession// 
  |  |  **|**  | ``~`` //Term2// 
  |  |  **|**  | ``<-`` //Name// 
  |  |  **|**  | //Term3// 
  | //Term1// | -> | //Term2// ``-o`` //Term1// 
  |  |  **|**  | //Term2// ``->`` //Term1// 
  |  |  **|**  | //Term2// ``**`` //Term1// 
  |  |  **|**  | ``let`` //Name// //OptSig// ``=`` //Term// ``in`` //Term// 
  |  |  **|**  | //Term2// 
  | //Term// | -> | ``\`` //Term2// ``->`` //Term// 
  |  |  **|**  | ``proc`` ``(`` //[ChanDec]// ``)`` //Proc// 
  |  |  **|**  | //Term1// 
  | //Proc1// | -> | //Act// 
  |  |  **|**  | ``(`` //[Proc]// ``)`` 
  | //Proc// | -> | //Proc1// //Proc// 
  |  |  **|**  | //Proc1// ``.`` //Proc// 
  |  |  **|**  | //Proc1// ``;`` //Proc// 
  |  |  **|**  | ``slice`` ``(`` //[ChanDec]// ``)`` //ATerm// ``as`` //Name// //Proc// 
  |  |  **|**  | //Proc1// 
  | //[Proc]// | -> | **eps**
  |  |  **|**  | //Proc// 
  |  |  **|**  | //Proc// ``|`` //[Proc]// 
  | //Act// | -> | //NewAlloc// 
  |  |  **|**  | //OptSplit// ``{`` //[ChanDec]// ``}`` 
  |  |  **|**  | //OptSplit// ``[`` //[ChanDec]// ``]`` 
  |  |  **|**  | //OptSplit// ``[:`` //[ChanDec]// ``:]`` 
  |  |  **|**  | ``send`` //Name// //ATerm// 
  |  |  **|**  | //Name// //OptSession// ``<-`` //ATerm// 
  |  |  **|**  | ``recv`` //Name// //VarDec// 
  |  |  **|**  | ``let`` //Name// //OptSig// ``<-`` //Name// 
  |  |  **|**  | ``let`` //Name// //OptSig// ``<=`` //ATerm// 
  |  |  **|**  | ``fwd`` //ASession// ``(`` //[ChanDec]// ``)`` 
  |  |  **|**  | ``fwd`` //Integer// //ASession// //Name// 
  |  |  **|**  | ``@`` //ATerm// //TopCPatt// 
  |  |  **|**  | ``let`` //Name// //OptSig// ``=`` //ATerm// 
  | //ASession// | -> | //ATerm// 
  | //OptAs// | -> | **eps**
  |  |  **|**  | ``as`` 
  | //OptSplit// | -> | ``split`` //Name// //OptAs// 
  |  |  **|**  | //Name// 
  | //TopCPatt// | -> | ``(`` //[ChanDec]// ``)`` 
  |  |  **|**  | ``{`` //[CPatt]// ``}`` 
  |  |  **|**  | ``[`` //[CPatt]// ``]`` 
  |  |  **|**  | ``[:`` //[CPatt]// ``:]`` 
  | //CPatt// | -> | //ChanDec// 
  |  |  **|**  | ``{`` //[CPatt]// ``}`` 
  |  |  **|**  | ``[`` //[CPatt]// ``]`` 
  |  |  **|**  | ``[:`` //[CPatt]// ``:]`` 
  | //[CPatt]// | -> | **eps**
  |  |  **|**  | //CPatt// 
  |  |  **|**  | //CPatt// ``,`` //[CPatt]// 
  | //OptSession// | -> | **eps**
  |  |  **|**  | ``:`` //RSession// 
  | //RSession// | -> | //Term// //OptRepl// 
  | //[RSession]// | -> | **eps**
  |  |  **|**  | //RSession// 
  |  |  **|**  | //RSession// ``,`` //[RSession]// 
  | //OptRepl// | -> | **eps**
  |  |  **|**  | ``^`` //ATerm// 
  | //CSession// | -> | ``.`` //Term1// 
  |  |  **|**  | **eps**
  | //AllocTerm// | -> | //Name// 
  |  |  **|**  | //Literal// 
  |  |  **|**  | ``(`` //Term// //OptSig// ``)`` 
  | //[AllocTerm]// | -> | **eps**
  |  |  **|**  | //AllocTerm// //[AllocTerm]// 
  | //NewPatt// | -> | ``[`` //[ChanDec]// ``]`` 
  |  |  **|**  | ``[:`` //[ChanDec]// ``:]`` 
  | //NewAlloc// | -> | ``new`` ``(`` //[ChanDec]// ``)`` 
  |  |  **|**  | ``new`` //NewPatt// 
  |  |  **|**  | ``new/`` ``(`` //Term// //OptSig// ``)`` //NewPatt// 
  |  |  **|**  | //OpName// //[AllocTerm]// //NewPatt// 


